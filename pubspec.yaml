name: multiplayer_snake
description: A server app using the shelf package and Docker.
version: 1.0.0
repository: https://github.com/OutdatedGuy/Multiplayer-Snake

environment:
  sdk: ^3.12.2

dependencies:
  shelf: ^1.4.2
  shelf_static: ^1.1.3
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  lints: ^6.0.0
